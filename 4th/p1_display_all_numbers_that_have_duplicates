user_numbers = []
for i in range(10):
    user_input = int(input(f"Enter number {i+1}: "))
    user_numbers.append(user_input)

duplicate_numbers = set([num for num in user_numbers if user_numbers.count(num) > 1])
print("Numbers with duplicates:", duplicate_numbers)
